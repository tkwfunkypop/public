# 引き継ぎメモ（アカウント間の共有ノート）

このファイルは **どのアカウント・どのセッションから来ても、状況がすぐ分かるようにするための共有ノート**です。
コンテナは毎回まっさらに起動し、チャット履歴はアカウントをまたいで共有されません。
**このリポジトリに書いてある内容だけが、もう片方のアカウントから見える情報です。**

- リポジトリ: <https://github.com/tkwfunkypop/public>（公開リポジトリなので誰のアカウントからでも閲覧可）
- 既定ブランチ: `claude/add-animejs-dependency-iqs0v1`

---

## 使い方（毎セッション）

**セッションの最初に**
1. このファイルを読む（「現在の状態」と「作業ログ」）。
2. `git log --oneline -20` で直近のコミットを確認する。
3. 未マージのPRがないか確認する。

**セッションの最後に**
1. 変更を commit & push する（push していない作業＝もう片方のアカウントからは存在しない作業）。
2. 下の「作業ログ」に1〜3行で追記して、それも push する。
3. 「現在の状態」の内容が変わったなら、そこも更新する。

---

## 現在の状態（最終更新: 2026-08-01）

### 中身の地図

| 場所 | 内容 |
|---|---|
| `lib/animations.js` | anime.js v4 のプリセット集（`AP.*`）。動きは全部ここ経由で付ける |
| `templates/lp-starter.html` | 新規LPの雛形。スクロール出現・カウントアップ・CTA脈打ち入り |
| `lp/ae-short-course.html` | 制作済みLP（AE短期講座） |
| `examples/` | anime.js の動作サンプル（CDN版 / ローカル版） |
| `adobe-cep-sample/` | Adobe CEP パネルのサンプル一式（`AP.*` をローカル同梱で使用） |
| `tools/ae-shortcuts/` | AE早見帳（Web版・PWA）。`manual/` に講座生向けマニュアル、`haifu.md` に配布セットまとめ |
| `tools/ae-shortcuts-app/` | AE早見帳のスマホアプリ版（Capacitor / Android） |
| `tools/kling/` | Kling 動画生成のスクリプト（`.env.example` を参照。**鍵は絶対にコミットしない**） |
| `tools/fish-audio/` | Fish Audio MCP の接続メモ（設定本体はリポジトリ直下の `.mcp.json`） |
| `.mcp.json` | プロジェクト共通のMCPサーバ設定。cloneすれば両アカウントに同じ設定が入る |

### 進行中の作業

- **WFAIA作品「はしっこビルの明日づくり」の仕上げ**（PR #16・ブランチ `claude/prompts-json-10cut-generation-cisssw`）:
  生成フェーズ完了（本編I2V 89本・CM14本・日英ナレ20本・字幕17本）。2026-08-01 に全素材の検品を実施し、
  結果は `works/hashikko-bill/qa/QA_REPORT_20260801.md`（リテイク確定11件・要人間確認14件）。
  ラフカットはデスクトップに英語版 `hashikko_honpen_v1.mp4`（296.7秒）と日本語版 `hashikko_honpen_v1_ja.mp4`（299.1秒）。
  **✅ WFAIAは提出済み**（2026-08-01 健太さん確認・締切7/31）。以降のリテイクは入賞時のワークフロー提出義務・アーカイブ品質向上・他コンテスト転用のための任意作業。
- PR #15 は 2026-08-01 にマージ済み（本ファイルと CLAUDE.md の運用ルールが既定ブランチに反映済み）。

### 確認用URL（ブラウザで見るとき）

`https://raw.githack.com/tkwfunkypop/public/<branch>/<path>`
最新を確実に見たいときは `rawcdn.githack.com` ＋ `<branch>` をコミットSHAに。

---

## メインアカウントへの引き継ぎ指示文（コピペ用）

このアカウントでの作業をメインアカウントの Claude セッションに引き継ぐときは、
新しいセッションの最初のメッセージとして次の文面をそのまま貼り付けてください。

```
このリポジトリ（tkwfunkypop/public）を、別アカウントのセッションから引き継ぐ。
作業を始める前に次を確認して:

1. HANDOFF.md を読む（「現在の状態」「進行中の作業」「作業ログ」を必ず見る）
2. CLAUDE.md のルールに従う（特に「アカウント間の情報共有」の節）
3. git log --oneline -20 で直近のコミットを確認する
4. 未マージPRを確認する: https://github.com/tkwfunkypop/public/pull/15
   （claude/account-info-sharing-wnulm8 → claude/add-animejs-dependency-iqs0v1、
    HANDOFF.md と CLAUDE.md の追加のみ、ドラフト、CIなし、レビューコメントなし、
    コンフリクトなし。ドラフト解除してマージするか判断してほしい）

確認できたら状況を要約して報告し、そのあと通常どおり作業を進めて。
セッションの最後には HANDOFF.md の作業ログに追記して push すること。
```

---

## 作業ログ（新しいものを上に）

### 2026-08-18 — Fish Audio MCP をリポジトリに接続
- `.mcp.json`（プロジェクトスコープ）に公式MCPサーバ `fish-audio`（`https://api.fish.audio/mcp` / HTTP / OAuth）を追加。cloneすれば両アカウントで設定を共有できる。APIキー不要なので鍵のコミット問題は起きない。
- 手順とハマりどころを `tools/fish-audio/README.md` にまとめた。
- **次にやること: ローカルのClaude Codeで `/mcp` からOAuthログインする。** リモート実行環境（web版）は `api.fish.audio` への外向き通信がポリシーで塞がれていて（CONNECT 403）、OAuthもブラウザが要るのでこのセッションでは認証まで到達できない。

### 2026-08-01 — はしっこビル引き継ぎ・PR #15マージ・全素材検品
- メインアカウント側セッションが引き継ぎを実施。PR #15 をドラフト解除してマージ（判断根拠: ドキュメント2ファイルのみ・コンフリクトなし・運用が既に機能）。
- 残回収物だったCM用Floyoアングル41枚の台帳追記で回収ゼロに。本編89+CM20を並列検品し `works/hashikko-bill/qa/QA_REPORT_20260801.md` に集約（リテイク確定11件: 実在文字の焼き込みが主因）。
- `edit/assemble.mjs` の `--ja` が英語版を上書きするバグを修正（`_ja.mp4` に分離）。日英両版のラフカットをデスクトップに生成済み。
- 次にやること: ①リテイク確定11件の再生成（健太さんのGO待ち・QAレポートの対処案参照） ②要人間確認14件の目視判断
- 同日追記: WFAIA提出済みを健太さんが確認（締切プレッシャー解消・リテイクは任意作業へ）

### 2026-07-31 — メインアカウントへの引き継ぎ資料を整備
- 本ファイルに「メインアカウントへの引き継ぎ指示文（コピペ用）」を追加。
  新しいセッションに貼り付けるだけで状況把握から始められるようにした。
- 「進行中の作業」に PR #15（ドラフト・未マージ、判断待ち）の詳細を明記。

### 2026-07-29 — アカウント間の情報共有ルールを整備
- この `HANDOFF.md` を新規作成。両アカウント共通の引き継ぎノートとして運用する。
- `CLAUDE.md` に「アカウント間の情報共有」ルールを追記（作業は必ず push、ログはここに残す）。
- リポジトリが公開設定であることを確認（別のGitHubアカウントからも閲覧可能）。

### 2026-07-03 — AE早見帳 v2.4 まわり
- 各エクスプレッションに「AIへの質問テンプレ」を追加。
- 講座生向けマニュアル（HTML/PDF）、配布セットまとめ `haifu.md`、解説動画リンクを追加。

### 2026-07-02 — AE早見帳 v2.0〜v2.2
- ショートカット134件・エクスプレッション42式にフル拡張。
- お気に入り／クイズ／カード直リンク／「💡目から鱗」機能を追加。

---

## 追記のテンプレ

```markdown
### YYYY-MM-DD — 見出し（何をしたか一言）
- やったこと / 変えたファイル
- 次にやること・引き継ぎ事項（あれば）
```
