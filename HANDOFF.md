# 引き継ぎメモ（アカウント間の共有ノート）

このファイルは **どのアカウント・どのセッションから来ても、状況がすぐ分かるようにするための共有ノート**です。
コンテナは毎回まっさらに起動し、チャット履歴はアカウントをまたいで共有されません。
**このリポジトリに書いてある内容だけが、もう片方のアカウントから見える情報です。**

- リポジトリ: <https://github.com/tkwfunkypop/public>（公開リポジトリなので誰のアカウントからでも閲覧可）
- 既定ブランチ: `claude/add-animejs-dependency-iqs0v1`

---

## 使い方（毎セッション）

**セッションの最初に**
1. このファイルを読む（「現在の状態」と「作業ログ」）。
2. `git log --oneline -20` で直近のコミットを確認する。
3. 未マージのPRがないか確認する。

**セッションの最後に**
1. 変更を commit & push する（push していない作業＝もう片方のアカウントからは存在しない作業）。
2. 下の「作業ログ」に1〜3行で追記して、それも push する。
3. 「現在の状態」の内容が変わったなら、そこも更新する。

---

## 現在の状態（最終更新: 2026-07-29）

### 中身の地図

| 場所 | 内容 |
|---|---|
| `lib/animations.js` | anime.js v4 のプリセット集（`AP.*`）。動きは全部ここ経由で付ける |
| `templates/lp-starter.html` | 新規LPの雛形。スクロール出現・カウントアップ・CTA脈打ち入り |
| `lp/ae-short-course.html` | 制作済みLP（AE短期講座） |
| `examples/` | anime.js の動作サンプル（CDN版 / ローカル版） |
| `adobe-cep-sample/` | Adobe CEP パネルのサンプル一式（`AP.*` をローカル同梱で使用） |
| `tools/ae-shortcuts/` | AE早見帳（Web版・PWA）。`manual/` に講座生向けマニュアル、`haifu.md` に配布セットまとめ |
| `tools/ae-shortcuts-app/` | AE早見帳のスマホアプリ版（Capacitor / Android） |
| `tools/kling/` | Kling 動画生成のスクリプト（`.env.example` を参照。**鍵は絶対にコミットしない**） |

### 進行中の作業

- 特になし（直近は AE早見帳 v2.4 のマニュアル・配布セット整備まで完了）

### 確認用URL（ブラウザで見るとき）

`https://raw.githack.com/tkwfunkypop/public/<branch>/<path>`
最新を確実に見たいときは `rawcdn.githack.com` ＋ `<branch>` をコミットSHAに。

---

## 作業ログ（新しいものを上に）

### 2026-07-29 — アカウント間の情報共有ルールを整備
- この `HANDOFF.md` を新規作成。両アカウント共通の引き継ぎノートとして運用する。
- `CLAUDE.md` に「アカウント間の情報共有」ルールを追記（作業は必ず push、ログはここに残す）。
- リポジトリが公開設定であることを確認（別のGitHubアカウントからも閲覧可能）。

### 2026-07-03 — AE早見帳 v2.4 まわり
- 各エクスプレッションに「AIへの質問テンプレ」を追加。
- 講座生向けマニュアル（HTML/PDF）、配布セットまとめ `haifu.md`、解説動画リンクを追加。

### 2026-07-02 — AE早見帳 v2.0〜v2.2
- ショートカット134件・エクスプレッション42式にフル拡張。
- お気に入り／クイズ／カード直リンク／「💡目から鱗」機能を追加。

---

## 追記のテンプレ

```markdown
### YYYY-MM-DD — 見出し（何をしたか一言）
- やったこと / 変えたファイル
- 次にやること・引き継ぎ事項（あれば）
```
